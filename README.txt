--[[
    Este programa baja la informacion del DataSheet en google drive y lo convierte en un objeto Lua para que el Addon Casa Cercone puedan leerlo

    Se compila con:
    dotnet publish -r win-x64 -c Release --self-contained false